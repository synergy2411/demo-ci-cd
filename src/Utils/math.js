export const sum = (n1, n2) => n1 + n2;

export const getFirstName = (username) => username.split(" ")[0];

export const getFriends = () => ["Monica", "Ross", "Joey", "Rachel"];

export const isDivisibleBy2 = (num) => num % 2 === 0;
